Sync and editor fixes. If a folder conversion to metadata-free mode was interrupted, the files it had already converted could return as duplicate notes on the next sync — the conversion now records each file before touching it. Saving to such a folder no longer scans the folder twice. The jump-to-end control now appears for notes that are long on screen, and switching the note-title rule finishes even if interrupted.
