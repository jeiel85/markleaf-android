A fix for note files whose metadata header contains an indented --- line, the shape other tools use for a long description. Markleaf ended the header there, so entries below it were never read as metadata and a stray --- was pasted into the note. Each sync saved that arrangement and read it back, so it worsened over time. Headers now end only on a line starting at the left margin.
